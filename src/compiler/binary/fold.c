void binary_fold( compiler* c, ir* l, opcode oc, ir* r )
{
	ir* out = vec_commit( c->parser->irs );
	// static const void* ops[ ] = { BINOPS( &&, X_BINOP ) };
	// oc -= op_add_i64;
	// value* lv = &l->value;
	// value* rv = &r->value;
	// goto *ops[ oc ];
}