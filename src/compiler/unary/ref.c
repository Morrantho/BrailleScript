stil void unary_ref( compiler* c, opcode oc, ir* rhs )
{
	// var* var = var_get( c, rhs->value.str );
	/* TODO: if( !var ) throw( BAD_REF ) */
	// out->type = ir_ref;
	// out->value.i64 = op->value.i64 + rhs->value.type; /* compounded */
	// out->idx = var->idx;
	// op_push( c, out->value.i64, var->idx, var->idx, 0 );
}